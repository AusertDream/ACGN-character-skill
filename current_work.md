# 当前工作进展

## 总体目标

当前的核心工作是调优 EventDetector 状态机，消除对话提取管线中由打字机效果引起的截断问题。旧管线在处理 7 个训练视频时的截断率为 23.3%（768 个事件中有 179 个被截断），目标是将截断率降到 5% 以下。管线已被拆分为两个阶段：Stage 1 由 frame_extractor.py 完成 OCR 驱动的事件检测和关键帧保存，Stage 2 由 batch_ocr.py 对保存的裁剪图像做离线 OCR。

## 已完成的工作

状态机的核心参数已经过一轮调优，`stable_frames_threshold` 从 3 调整为 5（即 2fps 下从 1.5 秒增加到 2.5 秒），`similarity_threshold` 从 0.6 降低为 0.5（降低替换判定的灵敏度，减少误判），`post_growth_stable_threshold` 从 5 调整为 10（即 2fps 下从 2.5 秒增加到 5 秒，给打字机效果完成后更长的稳定等待时间）。

frame_extractor.py 已从最初的 dummy_ocr/像素级检测切换为真正的 OCR 驱动检测。原来的方案用像素差异判断对话框内容变化，速度快（每个视频约 5 分钟）但无法理解文本内容，导致无法正确区分打字机增长和对话替换。现在 Stage 1 在每帧都调用 PaddleOCR 识别对话框文本，将识别结果送入状态机，由状态机基于文本内容做判断，虽然速度慢了很多（每个视频约 1 小时）但事件检测质量大幅提升。

截断问题的根因已定位并修复。`_is_text_replacement()` 方法在快速打字机增长场景下会误判——例如从"灼热的空"增长到"灼热的空气让肺部最后一丝生息也变得虚无"时，SequenceMatcher 计算的相似度只有 0.21，远低于 0.5 的阈值，因此被错误地当作对话替换而非打字机增长。修复方式是在调用 SequenceMatcher 之前先做三重检查：新文本是否包含旧文本作为子串、新文本的前缀是否与旧文本有超过 50% 的字符匹配、新文本是否包含任何最近 5 帧历史文本作为子串。只有这些检查都否定后才用 SequenceMatcher 比率做最终判定。

## 当前状态

一个测试任务正在后台运行（task bh2smlesk），内容是使用修复后的 OCR 驱动 frame_extractor 处理 ep18p2（崩坏三舰长线全剧情合集第十八节第二部分）。这个测试的目的是验证 `_is_text_replacement` 修复的效果，检查修复后的状态机是否能正确处理快速打字机增长而不产生截断。

## 下一步计划

等 ep18p2 的 Stage 1 测试完成后，需要依次执行以下工作。首先运行 batch_ocr.py 对 Stage 1 输出的裁剪图像做离线 OCR，得到最终的对话文本。然后用 analyze_truncation.py 分析截断率，与旧管线的 23.3% 进行对比，评估修复效果。如果截断率有显著改善（目标低于 5%），就扩展到全部 7 个训练视频运行新管线，验证修复在不同视频上的泛化性。如果截断率仍然不理想，需要进一步分析剩余的截断案例，判断是状态机参数还是 `_is_text_replacement` 逻辑还需要继续调整。

说话人识别的质量问题（名字框 OCR 产生 'y'、'f'、'-\`' 等噪声）优先级较低，计划在截断问题解决后再处理，解决方案可能涉及名字框 ROI 的重新校准或收紧 strict_whitelist 的模糊匹配规则。

## 训练视频清单

训练数据共 7 个视频文件，全部来自崩坏三舰长线全剧情合集。ep01 是第一节「仲夏幻夜」，讲述月下与舰长的初遇，是信息密度较高的章节。ep17 是第十七节「在长梦弥散之前」。ep18p1、ep18p2、ep18p3 是第十八节「当红月落幕之后」分三部分。ep18side 是第十八节主要支线「月下全回忆和彩蛋」，包含月下的核心独白与记忆，信息密度最高。ep19 是第十九节「牧场奇谭」，包含日常生活与归宿内容。

## 关键指标

旧管线的截断率基线为 23.3%（768 个事件中 179 个被截断）。目标截断率为低于 5%。处理速度方面，像素级检测方式约 5 分钟每个视频，OCR 驱动方式约 1 小时每个视频。Benchmark 验证结果（基于仲夏幻夜 10 分钟片段）显示 Recall 100%、Mean CER 6.64%、Duplicate rate 0%、Review rate 4.31%、False positives 0，所有指标均达标。

## 注意事项

所有处理命令需要在激活 paddleocr conda 环境后运行。视频文件和 OCR 输出产物位于 `training data/` 目录下，该目录已 gitignored。Stage 1 的输出保存在 `training data/ocr_output/` 或指定的输出目录中，包含 frames/、dialog_crops/、name_crops/ 子目录和 events_metadata.json。每次修改状态机参数后需要重新运行 Stage 1（耗时较长），但 Stage 2 可以快速重跑而无需重新处理视频。
