# ACGN-character-skill OCR dialogue extraction pipeline
# Python 3.10+
# PaddlePaddle GPU 需要单独安装，参考 README 中的安装说明

# Core dependencies
Pillow>=10.0
numpy>=1.24
av>=12.0
PyYAML>=6.0
opencv-python>=4.8
anthropic>=0.40.0

# PaddleOCR (install after paddlepaddle-gpu)
paddleocr>=2.7
